node_modules/
.devTools/
.vscode/sftp.json
logs/

# Config utilisateur (secrets, chemins locaux)
config/config.json
config/config.dev.json
# Base de données dans le dossier config
config/*.db
config/*.db-shm
config/*.db-wal
# Backups de la base de données
config/backups
# Certificats SSL dans le dossier config
config/*.crt
config/*.key

# Tests et couverture
coverage/
